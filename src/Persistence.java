/**
 * Write a function, persistence, that takes in a positive parameter
 * num and returns its multiplicative persistence, which is the number
 * of times you must multiply the digits in num until you reach a single digit.
 *
 *  example:  39 == 2 because 3*9 =27, 2*7 =14, 1*4 = 4
 */
/*
public class Persistence {

    public static int persistence(long n) {
        // your  999
        String myString = Long.toString(n);
        int result = 0;
        int counter = 0;
        int[] num = new int[myString.length()];

        for (int i = 0; i < myString.length(); i++){
             num[i] = toString(myString.charAt(i));
        }

        return result;
    }

}
*/
